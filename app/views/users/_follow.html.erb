<%= form_with(model: current_user.active_relationships.build, class: "inline") do |f| %>
  <%= hidden_field_tag :followed_id, @user.id %>
  <%= f.submit "フォローする", 
      class: "px-2 py-1 text-sm md:px-4 md:py-2 md:text-base bg-gray-800 text-white rounded cursor-pointer transition-colors duration-200 ease-in-out hover:bg-gray-400" %>
<% end %>